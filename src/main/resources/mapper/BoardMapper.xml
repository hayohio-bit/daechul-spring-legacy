<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.mapper.BoardMapper">

  <select id="getList" resultType="org.zerock.dto.BoardDTO">
    <![CDATA[
    SELECT * FROM board WHERE seq > 0 AND delflag = false ORDER BY seq DESC
    ]]>
  </select>

  <select id="getListWithPaging" resultType="org.zerock.dto.BoardDTO">
    <![CDATA[
    SELECT * 
    FROM board 
    WHERE delflag = false
    ORDER BY seq DESC
    LIMIT #{skip}, #{amount}
    ]]>
  </select>

  <insert id="insert">
    INSERT INTO board (writer, title, content)
    VALUES (#{writer}, #{title}, #{content})
  </insert>

  <insert id="insertSelectKey">
    <selectKey keyProperty="seq" order="AFTER" resultType="int">
      SELECT LAST_INSERT_ID()
    </selectKey>
    INSERT INTO board (writer, title, content)
    VALUES (#{writer}, #{title}, #{content})
  </insert>

  <select id="read" resultType="org.zerock.dto.BoardDTO">
    SELECT * FROM board WHERE seq = #{seq}
  </select>

  <delete id="delete">
    UPDATE board SET delflag = true WHERE seq = #{seq}
  </delete>

  <update id="update">
    UPDATE board 
    SET title = #{title},
        content = #{content},
        updatedate = NOW()
    WHERE seq = #{seq}
  </update>

  <select id="getTotalCount" resultType="int">
    SELECT COUNT(*) FROM board WHERE seq > 0 AND delflag = false
  </select>

  <update id="updateReplyCnt">
    UPDATE board SET replyCnt = replyCnt + #{amount} WHERE seq = #{bno}
  </update>
  
  <update id="updateHit">
    UPDATE board SET hit = hit + 1 WHERE seq = #{seq}
  </update>

</mapper>