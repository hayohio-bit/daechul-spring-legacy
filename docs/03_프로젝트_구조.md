# 📁 Part 3: 프로젝트 구조

## 1. 전체 폴더 구조 한눈에 보기

```
dc-SF/                                    ← 프로젝트 최상위 폴더
│
├── 📄 pom.xml                            ← Maven 설정 (라이브러리 관리)
├── 📄 setup_database.sql                 ← DB 테이블 생성 SQL
│
└── 📂 src/                               ← 소스 코드 폴더
    └── 📂 main/
        │
        ├── 📂 java/                      ← ⭐ Java 코드가 있는 곳
        │   └── 📂 org/zerock/
        │       ├── 📂 controller/        ← 요청을 받는 곳
        │       ├── 📂 service/           ← 실제 일을 처리하는 곳
        │       ├── 📂 mapper/            ← DB와 대화하는 곳
        │       ├── 📂 dto/               ← 데이터를 담는 그릇
        │       └── 📂 security/          ← 보안 설정
        │
        ├── 📂 resources/                 ← 설정 파일들
        │   ├── 📂 mapper/                ← SQL 쿼리 파일 (.xml)
        │   ├── 📄 mybatis-config.xml     ← MyBatis 설정
        │   └── 📄 log4j2.xml             ← 로그 설정
        │
        └── 📂 webapp/                    ← ⭐ 웹 관련 파일
            │
            ├── 📂 resources/             ← CSS, JS, 이미지
            │
            └── 📂 WEB-INF/               ← 중요! 외부 직접 접근 불가
                ├── 📄 web.xml            ← 웹 애플리케이션 설정
                ├── 📂 spring/            ← Spring 설정 XML
                │   ├── root-context.xml
                │   └── servlet-context.xml
                └── 📂 views/             ← JSP 화면 파일들
                    ├── 📄 home.jsp
                    ├── 📂 member/        ← 회원 관련 화면
                    ├── 📂 board/         ← 게시판 관련 화면
                    ├── 📂 admin/         ← 관리자 관련 화면
                    └── 📂 common/        ← 공통 화면 (헤더 등)
```

---

## 2. 각 폴더의 역할

### 2.1 Java 소스 코드 (src/main/java/org/zerock/)

```
┌─────────────────────────────────────────────────────────────────┐
│                         Java 패키지 구조                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  📂 controller/     ← "안내 데스크" 역할                         │
│  ├── HomeController.java       : 홈페이지 (/)                   │
│  ├── MemberController.java     : 회원 기능 (/member/*)          │
│  ├── BoardController.java      : 게시판 기능 (/board/*)         │
│  ├── ReplyController.java      : 댓글 기능 (/replies/*) REST API │
│  └── AdminController.java      : 관리자 기능 (/admin/*)         │
│                                                                 │
│  📂 service/        ← "실제 업무 담당자" 역할                     │
│  ├── MemberService.java        : 회원 기능 인터페이스            │
│  ├── MemberServiceImpl.java    : 회원 기능 실제 구현            │
│  ├── BoardService.java         : 게시판 기능 인터페이스          │
│  ├── BoardServiceImpl.java     : 게시판 기능 실제 구현          │
│  ├── ReplyService.java         : 댓글 기능 인터페이스            │
│  └── ReplyServiceImpl.java     : 댓글 기능 실제 구현            │
│                                                                 │
│  📂 mapper/         ← "DB 전문가" 역할                           │
│  ├── MemberMapper.java         : 회원 DB 작업 인터페이스         │
│  ├── BoardMapper.java          : 게시판 DB 작업 인터페이스       │
│  └── ReplyMapper.java          : 댓글 DB 작업 인터페이스        │
│                                                                 │
│  📂 dto/            ← "데이터를 담는 상자" 역할                   │
│  ├── MemberDTO.java            : 회원 정보 담는 클래스           │
│  ├── BoardDTO.java             : 게시글 정보 담는 클래스         │
│  ├── ReplyDTO.java             : 댓글 정보 담는 클래스           │
│  ├── BoardListPaginDTO.java   : 게시글 페이징 정보 클래스       │
│  └── ReplyListPaginDTO.java   : 댓글 페이징 정보 클래스         │
│                                                                 │
│  📂 security/       ← "보안 담당자" 역할                          │
│  ├── SecurityConfig.java       : 보안 전체 설정                 │
│  ├── CustomUserDetailsService.java : 로그인 시 회원 정보 조회   │
│  ├── CustomLoginSuccessHandler.java : 로그인 성공 시 처리       │
│  ├── Custom403Handler.java     : 접근 거부 시 처리              │
│  └── PasswordEncoderConfig.java : 비밀번호 암호화 설정          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 설정 파일들 (src/main/resources/)

```
┌─────────────────────────────────────────────────────────────────┐
│                           설정 파일들                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  📂 mapper/                                                     │
│  ├── MemberMapper.xml    ← 회원 관련 SQL 쿼리들                  │
│  ├── BoardMapper.xml     ← 게시판 관련 SQL 쿼리들                │
│  └── ReplyMapper.xml     ← 댓글 관련 SQL 쿼리들                  │
│                                                                 │
│  📄 mybatis-config.xml   ← MyBatis 전체 설정                     │
│     (별칭(alias) 설정, 옵션 설정)                                │
│                                                                 │
│  📄 log4j2.xml           ← 로그 출력 설정                        │
│     (어떤 정보를 어디에 기록할지)                                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.3 웹 관련 파일들 (src/main/webapp/)

```
┌─────────────────────────────────────────────────────────────────┐
│                          웹 파일 구조                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  📂 resources/           ← 정적 파일 (CSS, JS, 이미지)           │
│  ├── css/                                                       │
│  ├── js/                                                        │
│  └── images/                                                    │
│                                                                 │
│  📂 WEB-INF/             ← 보안 폴더 (직접 URL 접근 불가)        │
│  │                                                              │
│  ├── 📄 web.xml          ← 웹 애플리케이션 핵심 설정             │
│  │   • 필터 설정 (인코딩, 보안)                                  │
│  │   • 서블릿 설정 (DispatcherServlet)                          │
│  │   • Spring 설정 파일 위치 지정                               │
│  │                                                              │
│  ├── 📂 spring/          ← Spring 설정 XML 파일들               │
│  │   ├── root-context.xml    ← DB, 서비스, 보안 설정            │
│  │   └── servlet-context.xml ← 웹 MVC 설정                      │
│  │                                                              │
│  └── 📂 views/           ← JSP 화면 파일들                       │
│      ├── home.jsp        ← 메인 홈페이지                        │
│      │                                                          │
│      ├── 📂 member/      ← 회원 관련 화면                        │
│      │   ├── login.jsp   ← 로그인 페이지                        │
│      │   ├── join.jsp    ← 회원가입 페이지                      │
│      │   └── access-denied.jsp ← 접근 거부 페이지               │
│      │                                                          │
│      ├── 📂 board/       ← 게시판 관련 화면                      │
│      │   ├── list.jsp    ← 글 목록 (페이징 포함)                │
│      │   ├── view.jsp    ← 글 상세보기 (댓글 기능 포함)          │
│      │   ├── write.jsp   ← 글 작성                              │
│      │   └── update.jsp  ← 글 수정                              │
│      │                                                          │
│      ├── 📂 admin/       ← 관리자 관련 화면                      │
│      │   ├── main.jsp    ← 관리자 메인                          │
│      │   └── member/     ← 회원 관리 화면                       │
│      │       ├── list.jsp                                       │
│      │       └── detail.jsp                                     │
│      │                                                          │
│      └── 📂 common/      ← 공통으로 쓰이는 화면                   │
│          ├── header.jsp  ← 상단 메뉴 (모든 페이지에 포함)        │
│          └── footer.jsp  ← 하단 (저작권 등)                     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. 레이어(계층) 구조 이해하기

이 프로젝트는 **MVC 패턴**을 사용해요. 각 역할을 분리해서 코드가 깔끔해져요!

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   👤 사용자 (브라우저)                                           │
│        │                                                        │
│        ▼                                                        │
│   ┌─────────────────────────┐                                   │
│   │    🏛️ Controller 계층   │  ← "접수처"                        │
│   │    (요청을 받아서 전달)   │     어떤 일을 해야 하는지 파악     │
│   └───────────┬─────────────┘                                   │
│               │                                                 │
│               ▼                                                 │
│   ┌─────────────────────────┐                                   │
│   │    ⚙️ Service 계층       │  ← "실무자"                        │
│   │    (실제 업무 처리)       │     진짜 일을 처리                 │
│   └───────────┬─────────────┘                                   │
│               │                                                 │
│               ▼                                                 │
│   ┌─────────────────────────┐                                   │
│   │    🗄️ Mapper 계층        │  ← "창고 담당자"                   │
│   │    (DB와 통신)           │     데이터 저장/조회              │
│   └───────────┬─────────────┘                                   │
│               │                                                 │
│               ▼                                                 │
│   ┌─────────────────────────┐                                   │
│   │    🐬 Database (MySQL)   │  ← "창고"                         │
│   │    (데이터 저장소)        │     모든 정보 보관               │
│   └─────────────────────────┘                                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 각 계층의 역할 비유

| 계층 | 파일 위치 | 음식점 비유 | 하는 일 |
|------|----------|------------|--------|
| Controller | controller/*.java | 홀 직원 | 손님 주문 받기 |
| Service | service/*.java | 주방장 | 요리 만들기 (핵심 로직) |
| Mapper | mapper/*.java +*.xml | 재료 창고 담당 | 재료 가져오기/저장 |
| DTO | dto/*.java | 접시 | 음식(데이터) 담기 |
| View | views/*.jsp | 테이블 | 손님에게 음식 보여주기 |

---

## 4. 파일 명명 규칙

일관된 이름 규칙을 지키면 파일 찾기가 쉬워요!

```
┌─────────────────────────────────────────────────────────────────┐
│                         이름 짓기 규칙                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  📂 패키지명        : 소문자, 점(.)으로 구분                      │
│     예) org.zerock.controller                                   │
│                                                                 │
│  📄 Controller      : *Controller.java                          │
│     예) BoardController.java, MemberController.java,            │
│         ReplyController.java (REST API)                          │
│                                                                 │
│  📄 Service 인터페이스: *Service.java                            │
│     예) BoardService.java, ReplyService.java                     │
│                                                                 │
│  📄 Service 구현체   : *ServiceImpl.java                         │
│     예) BoardServiceImpl.java, ReplyServiceImpl.java            │
│                                                                 │
│  📄 Mapper 인터페이스: *Mapper.java                              │
│     예) BoardMapper.java, ReplyMapper.java                      │
│                                                                 │
│  📄 Mapper XML       : *Mapper.xml (인터페이스와 같은 이름)       │
│     예) BoardMapper.xml, ReplyMapper.xml                        │
│                                                                 │
│  📄 DTO              : *DTO.java                                 │
│     예) BoardDTO.java, MemberDTO.java, ReplyDTO.java             │
│                                                                 │
│  📄 페이징 DTO       : *ListPaginDTO.java                        │
│     예) BoardListPaginDTO.java, ReplyListPaginDTO.java          │
│                                                                 │
│  📄 JSP              : 소문자, 하이픈(-) 사용                     │
│     예) list.jsp, write.jsp, access-denied.jsp                  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 5. 중요한 설정 파일 요약

| 파일 | 위치 | 역할 |
|------|------|------|
| pom.xml | 프로젝트 루트 | 라이브러리 의존성 관리 |
| web.xml | WEB-INF/ | 웹 애플리케이션 진입점 설정 |
| root-context.xml | WEB-INF/spring/ | DB, 서비스 빈(Bean) 설정 |
| servlet-context.xml | WEB-INF/spring/ | 웹 MVC, 뷰 리졸버 설정 |
| mybatis-config.xml | resources/ | MyBatis 전체 설정 |
| SecurityConfig.java | security/ | Spring Security 보안 설정 |

---

👉 **다음: Part 4: 데이터베이스 설계** - 테이블 구조와 SQL 쿼리 상세 설명
